get?:
  responses:
    200:
      body:
        text/plain:
          example: !include examples/example.txt
    201?:
      body:
        application/json:
          example: !include examples/example.txt
