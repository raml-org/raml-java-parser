{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"type": "object",
	"title": "change log level",
	"properties": {
		"newLevel": {
			"type": "string",
			"description": "the new level to be set",
			"enum": [
				"OFF",
				"ERROR",
				"WARN",
				"INFO",
				"DEBUG",
				"TRACE"
			]
		},
		"createFlag": {
			"description": "optional parameter to create a logger in case it does not exist (to avoid typo errors for existing loggers)",
			"type": "boolean"
		},
		"hoursToReset": {
			"description": "delay after which time the logger is set back to it's original log-level'",
			"type": "integer"
		}
	},
	"required": [
		"newLevel"
	]
}